# 🤖 AI Tools Discovery Website

แพลตฟอร์มศูนย์รวมเครื่องมือ AI (AI Tools & Prompts) เพื่อช่วยอำนวยความสะดวกในการค้นหาข้อมูล AI แอปพลิเคชันที่เหมาะสมกับการทำงาน พร้อมทั้งมีคำสั่งประโยคตัวอย่าง (Prompts) ไว้ให้ศึกษาและนำไปใช้งานต่อ

---

## ⭐️ ระบบนี้ทำอะไรได้บ้าง?

**1. สำหรับผู้ใช้งานทั่วไป (User)**
*   ค้นหาเครื่องมือ AI ตามหมวดหมู่ (เช่น ข้อความ, รูปภาพ, วิดีโอ, เสียง)
*   ดูรายละเอียดของ AI แต่ละตัว (ราคา, ลิงก์เข้าใช้งาน, รูปภาพประกอบ)
*   ดูตัวอย่างและคัดลอกคำสั่ง Prompts พื้นฐานเพื่อนำไปประยุกต์ใช้งาน

**2. สำหรับผู้ดูแลระบบ (Admin)**
*   **รหัสผ่านเข้าสู่ระบบ:** ข้อมูลล็อกอินเริ่มต้นคือ `depa@4321` (เข้าผ่านไอคอนรูป ⚙️ ฟันเฟืองมุมขวาบนของเว็บ)
*   **สิทธิ์การเข้าถึง:** เมื่อล็อกอินผ่าน จะปรากฏเมนู "เข้าสู่ระบบจัดการ Data" 
*   **จัดการ AI Tools:** 
    *   เพิ่มเครื่องมือ AI ตัวใหม่บนหน้าเว็บไซต์
    *   แก้ไขเนื้อหา, ราคา, เปลี่ยนหมวดหมู่, หรือลิงก์ไปยังเว็บไซต์นั้นๆ
    *   สั่งลบเครื่องมือ AI ที่ไม่เกี่ยวข้องแล้วออกจากระบบ
*   **จัดการ Prompts:**
    *   เพิ่มชุดคำสั่ง Prompt พร้อมแยกประเภทตาม AI แต่ละตัว (เช่น ชุดคำสั่งสำหรับ ChatGPT, ชุดคำสั่งสำหรับ Midjourney)
    *   แก้ไขหรือลบคำสั่ง Prompt 

---

## 🚀 การนำโปรเจกต์นี้ไปใช้งานต่อ (ติดตั้งบนคอมพิวเตอร์เครื่องใหม่)

หากคุณดาวน์โหลดระบบนี้ (Source Code) ไปใช้งาน และมีไฟล์ข้อมูลฐานข้อมูล (Database) ที่สำรองไว้แล้วชื่อว่า `Dump20260301.sql` เตรียมไว้ ให้ทำตาม 3 ขั้นตอนนี้เท่านั้น

### ขั้นตอนที่ 1: การนำเข้าฐานข้อมูล (Database Import) ด้วย MySQL Workbench

การจะให้ระบบมีข้อมูล AI Tools ปรากฏขึ้นมาเหมือนเครื่องต้นฉบับ คุณต้องดึงข้อมูลจากไฟล์ `.sql` กลับเข้าไปในโปรแกรมจัดการฐานข้อมูลก่อน

1. ติดตั้งและรันโปรแกรมฐานข้อมูลในเครื่อง (แนะนำใช้ **XAMPP** ควบคู่กับ **MySQL Workbench**) 
2. เปิดโปรแกรม **MySQL Workbench** แล้วเชื่อมต่อเข้ากับฐานข้อมูลหลักในเครื่องคุณ
3. สังเกตแถบซ้ายมือ ในส่วนของหัวข้อ **MANAGEMENT** ให้คลิกที่ **`Data Import/Restore`**
4. ในหน้าจอตรงกลาง (Import Options) ให้เลือกหัวข้อล่างที่เขียนว่า **`Import from Self-Contained File`**
5. กดปุ่ม `...` ที่อยู่ต่อท้ายช่อง เพื่อไปยังโฟลเดอร์ที่คุณเก็บไฟล์ **`Dump20260301.sql`** เอาไว้ แล้วเลือกไฟล์นั้น
6. เลื่อนลงมามองหาช่องคำว่า **`Default Target Schema`** ให้กดปุ่ม `New...` แล้วพิมพ์สร้างฐานข้อมูลชื่อ **`ai_tools_db`** ลงไป (ถ้าไม่มีปุ่ม New ให้พิมพ์ชื่อฐานข้อมูลลงไปตรงๆ)
7. กดปุ่ม **`Start Import`** ที่มุมขวาล่าง 
8. รอจนระบบโชว์ว่า `Import completed` ถือเป็นการย้ายข้อมูลสำเร็จ 100%

### ขั้นตอนที่ 2: รันระบบเซิร์ฟเวอร์หลังบ้าน (Backend)

ต้องเปิดระบบหลังบ้าน (API) ทิ้งไว้เพื่อเป็นสะพานเชื่อมระหว่างเว็บไซต์กับฐานข้อมูล MySQL ของคุณ

1. เปิดโปรแกรม Terminal (หรือ Command Prompt) ในคอมพิวเตอร์
2. เข้าไปที่โฟลเดอร์หลังบ้านด้วยคำสั่ง:
   ```bash
   cd "C:\...(โฟลเดอร์ที่เก็บงาน)...\AI Tools Discovery Website\backend"
   ```
3. ติดตั้งแพ็กเกจ (ทำเฉพาะครั้งแรกหลังพึ่งโคลนงานมาใหม่):
   ```bash
   npm install
   ```
4. เปิดเซิร์ฟเวอร์:
   ```bash
   npm run dev
   ```
   *(รอจนกว่าระบบจะขึ้นว่า Server is running on port 5000 ห้ามปิดหน้าจอ Terminal นี้เด็ดขาด)*

### ขั้นตอนที่ 3: เปิดหน้าเว็บไซต์ (Frontend)

1. **เปิดหน้าจอ Terminal ขึ้นมาใหม่ 1 อัน** (แยกกับอันเดิม)
2. เข้าไปที่โฟลเดอร์หลักของโปรเจกต์:
   ```bash
   cd "C:\...(โฟลเดอร์ที่เก็บงาน)...\AI Tools Discovery Website"
   ```
3. ติดตั้งแพ็กเกจ:
   ```bash
   npm install
   ```
4. รันระบบเว็บไซต์:
   ```bash
   npm run dev
   ```
5. ระบบจะแสดง URL พื้นที่หน้าเว็บ เช่น `http://localhost:5173/` 
6. คัดลอกลิงก์นั้นไปเปิดบนเว็บเบราว์เซอร์ (Google Chrome, Edge หรืออื่นๆ) คุณก็จะพบกับหน้าเว็บไซต์ที่มีข้อมูลครบถ้วนเหมือนต้นฉบับครับ!